@model Ateliex.Areas.Cadastro.Models.ModeloRecursoTipoDescricao

@{
    var info = Model.GetInfo();

    var parent = "ModeloRecursoTipoDescricao";

    var viewData = new ViewDataDictionary(ViewData) { { "Parent", parent } };
}

<section>
    <h7>Não há mais detalhes de descrição de tipo de recurso</h7>
</section>
