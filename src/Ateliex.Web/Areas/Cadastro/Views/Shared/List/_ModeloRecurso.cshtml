@model IEnumerable<Ateliex.Areas.Cadastro.Models.ModeloRecurso>

@{
    var parent = (string?)ViewData["Parent"];
}

<div class="row table-responsive">
    <table class="table table-striped table-hover">
        <thead class="table-light">
            <tr>
                <th>#</th>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                @if (parent != "Modelo")
                {
                    <th>
                        @Html.DisplayNameFor(model => model.Modelo)
                    </th>
                }
                <th>
                    @Html.DisplayNameFor(model => model.Descricao)
                </th>
                @if (parent != "ModeloRecursoTipo")
                {
                    <th>
                        @Html.DisplayNameFor(model => model.Tipo)
                    </th>
                }
                <th>
                    @Html.DisplayNameFor(model => model.Custo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Unidades)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CustoPorUnidade)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th>
                        <a asp-area="Cadastro" asp-controller="ModeloRecurso" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-link-45deg"></i></a>
                        <a asp-area="Cadastro" asp-controller="ModeloRecurso" asp-action="Edit" asp-route-id="@item.Id" asp-route-parent="@parent"><i class="bi bi-pencil"></i></a>
                        <a asp-area="Cadastro" asp-controller="ModeloRecurso" asp-action="Delete" asp-route-id="@item.Id" asp-route-parent="@parent" class="link-danger"><i class="bi bi-trash"></i></a>
                    </th>
                    <th>
                        @Html.DisplayFor(modelItem => item.Id)
                    </th>
                    @if (parent != "Modelo")
                    {
                        <td>
                            <a asp-area="Cadastro" asp-controller="Modelo" asp-action="Details" asp-route-id="@item.ModeloId">
                                @Html.DisplayFor(modelItem => item.Modelo.Nome)
                                <i class="bi bi-link-45deg"></i>
                            </a>
                        </td>
                    }
                    <td>
                        @Html.DisplayFor(modelItem => item.Descricao)
                    </td>
                    @if (parent != "ModeloRecursoTipo")
                    {
                        <td>
                            <a asp-area="Cadastro" asp-controller="ModeloRecursoTipo" asp-action="Details" asp-route-id="@item.TipoId">
                                @Html.DisplayFor(modelItem => item.Tipo.Nome)
                                <i class="bi bi-link-45deg"></i>
                            </a>
                        </td>
                    }
                    <td>
                        @Html.DisplayFor(modelItem => item.Custo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Unidades)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CustoPorUnidade)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

