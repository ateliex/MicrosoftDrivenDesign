@model Ateliex.Data.Entity

@{
    var info = Model.GetInfo();

    ViewData["Title"] = $"Criar - {info.SingleName}";

    var parent = (string?)ViewData["Parent"];
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index">@info.SingleName</a></li>
        <li class="breadcrumb-item active" aria-current="page">Criar</li>
    </ol>
</nav>

<section>
    <h1>Criar @info.SingleName</h1>

    <form asp-action="Create" class="container">
        @*<h5>Novo</h5>*@

        <partial name="_Form" model="Model" />

        <input type="hidden" asp-for="@parent" />

        <p>
            <button type="submit" class="btn btn-primary"><i class="bi bi-save"></i>Salvar</button>
        </p>
    </form>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
